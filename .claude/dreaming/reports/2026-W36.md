Confirmed: **zero new commits in the last 7 days** — this pass is purely a status-resume with the W36 report slot to fill. No TaskCreate needed. Writing the report.

# vmm-rada-web-ui dreaming pass — 2026-W36

Date: 2026-09-06
Branch surveyed: main @ baf2074
Commits examined: 18 (last 30 days); 50 (last 60/90 days) — **0 new commits since 2026-08-30**
PRs examined: 20 most recent merged via git log (gh API auth unavailable this pass; reconstructed from `git log --merges` and `git log` of `src/`/docs/ paths)

## TL;DR

- **Zero new activity in 7 days.** Last commit on `main` is still `baf2074` (2026-08-23, PR #146 / self-learn). This pass is a **dormant-week resume**, not a fresh audit — the W35 report's findings are still the current state of play.
- **Two of W35's "open questions" are now resolved by time, not action**: the `test-generator` memory mtime (2026-07-27) crossed the 30-day mark (now ~41 days) but is still inside the 60-day staleness threshold; the `api-contract.md` mtime (2026-07-31) is still unchanged — both are still genuine items, just with sharper numbers.
- **W35's chronic CI-coverage item** (Check 7 of `/housekeeping`) is now **5+ weeks dormant** with no new movement. This is the only meaningful new signal in the pass.
- **All four context-essentials architecture rules remain clean** — re-verified in this pass; no new violations introduced since W35.

## 1. Context-essentials drift

- **Rule 1 (no `fetch` in components)**: ✅ no violations.
  `grep -rE "fetch\(" src/components/` → 0 matches; all 7 `fetch(` calls live in `src/api.js` (lines 96, 107, 124, 134, 143, 158, 182). Confidence: **high**.
- **Rule 2 (`src/api.js` is the adapter boundary; `onEvent(type, event)`)**: ✅ no violations. No SSE-handling code outside `src/api.js`. Confidence: **high**.
- **Rule 3 (`App.jsx` owns all state)**: ✅ no violations.
  `grep -rE "setCurrent|setMessages|setStage|setHistory|setMetadata" src/components/` → 0 matches. The `updateLast` / outer-spread-inner-mutate pattern from PR #126 is still the only state mutation path. Confidence: **high**.
- **Rule 4 (`react-markdown` is the only renderer)**: ✅ no violations.
  `dangerouslySetInnerHTML` and `innerHTML` both have zero matches in `src/`; the only literal hits are the explicit anti-pattern comments in `src/components/Markdown.test.jsx:5,31`. No competing markdown renderers (`remark`/`markdown-it`/`marked`/`html-react-parser` → 0 matches). Confidence: **high**.
- **Banned pattern: `--no-verify`**: ✅ zero matches in `git log --since="60 days ago" --grep="--no-verify"`. The only literal hits in the working tree are the anti-pattern seed in `_patterns/anti-patterns.md:6` and `context-essentials.md:60` itself. Confidence: **high**.
- **Banned pattern: `innerHTML` string matching for XSS safety**: ✅ no violations; the Markdown test file documents the prohibition in two places. Confidence: **high**.
- **Banned pattern: TypeScript**: ✅ `grep -E "\.tsx|\.ts\b"` in `src/` returns 0 files. Confidence: **high**.
- **Self-Learning Hard Rules (added 2026-08-23)**: ✅ still no amendment/force-push commits in the 7 days since W35 (and none in the 14 days total since the rule landed). Confidence: **high** (still light data, but trend continues).

**Net for W36: zero new violations vs. W35.** This is a fully clean re-check.

## 2. Recurring `/fix-review` themes

- **None new.** No PRs have been merged in the 7 days since W35, so no new review signal has arrived. The W35 conclusion (single-reviewer gate routing cleanly for non-`src/` diffs; no recurring theme to promote) still holds.
- The last substantive src-touching PR was #127 (strategy picker, 2026-08-06). That was clean. **No finding has emerged in ~31 days** of merged PR history worth promoting to a context-essentials rule or new skill specialization. Confidence: **high** that this is genuine, not a detection gap — the dreaming + self-learn + housekeeping PRs have all been intentionally low-surface, and the one substantive src-PR (strategy picker) was reviewed under full 3-model fan-out and came back clean.

## 3. Stale plans

- **No stale plans.** `.claude/plans/` contains only `README.md`. PR #128's plan cleanup (2026-08-06) has held for 31 days. Confidence: **high**.

## 4. Agent-memory health

- **`test-generator/MEMORY.md` + `vmm-rada-web-ui-quirks.md`**: only agent memory present in `.claude/agent-memory/`.
  - `vmm-rada-web-ui-quirks.md` mtime: **2026-07-27** → now **~41 days old** (still inside the 60-day staleness threshold, but moved past the 30-day mark noted in W35).
  - 3 entries, all project-specific Vitest/RTL quirks. No contradictions with context-essentials. No duplicates across other agents.
  - All 3 entries remain load-bearing:
    - "vi is global" → applies to every test file written by the agent.
    - "Final Answer" ambiguous match → applies to Stage3 tests.
    - "Stage components are plain prop-driven" → shapes the single-`render()` test pattern.
  - **Suggest**: recheck at W37 (47 days). If no test work has landed by then, archive the file or prune entries that haven't been exercised in 60+ days. The memory is healthy, but it's drifting from "actively used" toward "reference shelf."
- **No memories** under `bug-fixer`, `code-simplifier`, `docs-maintainer`, `security-reviewer`, `tech-lead`, or `static-analysis`. Steady state, same as W35. Confidence: **medium**.

## 5. Skill / agent inventory

- **11 skills** under `.claude/skills/` — all defined in `CLAUDE.md`. Cross-reference clean.
- **9 agents** under `.claude/agents/` — all defined in `CLAUDE.md`. No orphan agents introduced or dropped.
- **Overlap check**: same as W35 (no new findings).
- **`docs-maintainer` still reportedly broken** per `project_docs_maintainer_agent_broken.md`. Not re-checked this pass. Not blocking.

## 6. Backend contract drift

- **`docs/api-contract.md`** (mtime **2026-07-31**) — **unchanged since W35 (7 days)**. The drift flagged in W35's open questions (stage2Kind replay derivation, loading derivation) is still present in code (PRs #98, #99, #109, #116) but unaccounted for in prose. Severity: **medium** — frontend-only derived-state changes, not wire contract, but documentation drift is a known recurring `/fix-review` theme (per W35's own analysis of housekeeping patterns).

> [applied 2026-09-08: verified — false alarm, same as W35 §6. docs/streaming.md and docs/architecture.md already document these fixes accurately. See 2026-W35.md's annotation for full evidence.]
- **`docs/streaming.md`** (mtime **2026-07-28**) — unchanged since W35.
- **`docs/architecture.md`** (mtime **2026-07-29**) — unchanged since W35.
- **`docs/development-workflow.md`** (mtime **2026-07-28**) — unchanged since W35.
- **`docs/user-guide.md`** (mtime **2026-07-31**) — unchanged since W35.
- **No new backend contract changes observed.** Zero `src/` commits in the last 7 days. The drift posture from W35 carries forward verbatim. Confidence: **high** (status only, no new signal).

## 7. Patterns I noticed

- **Project is in pure steady state.** 31 days since the last substantive src-touching PR (#127 strategy picker). 7 days since the last commit on `main` (baf2074 / PR #146 self-learn). This is the longest stretch of inactivity in the 90-day window — worth noting not as a problem, but as a baseline reference. Possibilities: dependency-bump-driven lull (the next batch of dependabot PRs would be the first activity), or the backlog has genuinely been cleared and `/backlog` is the next signal source.
- **Strategy picker shipped; strategy picker integration is the only large feature mentioned in W35's open questions as "single remaining large unresolved feature."** It is now in `main` (PR #127, 2026-08-06). The backlog is structurally empty. If no new issues have arrived, this is a feature-complete-and-quiescent moment. Confidence: **medium** (could not verify against the actual open-issue list since `gh` is unauthenticated this pass).
- **W35's CI coverage item is now chronic**: 5+ weeks without movement. Per `feedback_dependabot_merge_no_ask`, that's not the same urgency class as a dependabot merge queue, but it is a real open item that's been on every weekly report since W30. Suggest a one-time decision: either schedule it as a focused `/ship` task, or move it from PLANNED to OUT-OF-SCOPE so the dreaming report stops re-flagging it. Confidence: **high**.

> [planned 2026-09-08: user chose "schedule as /ship task" — .claude/plans/2-dreaming-w36-ci-coverage.md; awaiting /backlog]
- **Cross-project `fix-review` agent config drift**: the 2026-08-23 session-log entry mentions adding `agy` to `~/.claude/skills/lib/agents.sh` and `.claude/skills/fix-review/config.yaml` in a `chore/add-agy-to-fix-review-external-agents` branch on this repo. That branch's commit was not found on `main`, so the W35 audit of "agy is configured" still doesn't apply here — but the cross-project pattern from CLAUDE.md memory ("after any Ollama cloud-model retirement, audit `fix-review` config and `session-end.sh` model lists in the same commit") suggests a config-sync sweep is on the horizon. Not actionable in this pass, just noted.

> [skipped 2026-09-08: report explicitly says "not actionable in this pass" — no action]

## 8. What I couldn't tell (need manual review)

- **Whether `gh` authentication is broken for this session or just transient.** `gh pr list` returned HTTP 401. The reconstruction via `git log` is sufficient for the dreaming audit, but if a future pass also fails `gh auth`, the workflow may need a fallback marker in `dreaming.sh`. Low priority.

  > [applied 2026-09-08: verified — was transient. `gh auth status` shows a healthy, active login this session; `gh pr`/`gh issue` calls have worked throughout. No fallback marker needed.]

- **Whether any open issue exists on `valpere/vmm-rada-web-ui` that the dreaming pass should surface.** `gh issue list` not callable. The W35 backlog-emptiness claim rests on the session-log saying "backlog is structurally empty" rather than a fresh query. Worth a one-time `gh issue list --state open` recheck.

  > [applied 2026-09-08: ran `gh issue list --state open` — 0 open issues. Confirms the backlog-empty claim with a fresh query, not just session-log inference.]
- **Content freshness of `docs/streaming.md` / `docs/architecture.md` vs. derived-state changes** — same as W35 §8. Unchanged.
- **`docs-maintainer` broken-ness** — same as W35 §8. Unchanged.
- **`/apply-dreaming` adoption cadence** — W35 reported "one apply per week, healthy." This pass cannot confirm the W35→W36 apply cadence because no PR landed in the last 7 days, which is itself the answer (no findings to apply). Confidence: **high** in that interpretation.
- **CI coverage gap** — same as W35 §8 but now with a sharper number (~5 weeks dormant). See §7 for the PLANNED→OUT-OF-SCOPE suggestion.

> [planned 2026-09-08: see §7 annotation — plan created, awaiting /backlog]
- **Whether `~/.claude/agents/dependabot-reviewer.md` is still healthy.** Not touched in this pass. W35's posture (assume healthy until contradicted) carries forward.
