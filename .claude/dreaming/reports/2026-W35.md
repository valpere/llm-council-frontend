Skip task tracking — single-pass report generation. I have enough data. `fetch` calls all live in `src/api.js` (correct boundary). `--no-verify` zero matches. Docs mtimes show api-contract.md updated 2026-07-31 — no updates since PRs #109 (stage2Kind) and #99/#100 (loading derivation), but those were merged weeks after those docs updates. Let me write the report.

# vmm-rada-web-ui dreaming pass — 2026-W35

Date: 2026-08-30
Branch surveyed: main @ baf2074
Commits examined: ~45 (last 90 days)
PRs examined: 20 most recent merged (state=merged, limit=20)

## TL;DR

- **0 active context-essentials violations** — all four Layer 1 rules
  verified across the entire 90-day window. Architecture is stable.
- **`/fix-review` is operating in pure steady-state** — round_1 single
  reviewer (REVIEWER_COUNT=1) for non-`src/` diffs has produced 0
  findings on the last ~8 housekeeping PRs (W30–W34 dreaming reports,
  self-learn). Diff-shape gate is routing correctly; no theme to act on.
- **Backend-docs sync is the only outstanding risk** —
  `docs/streaming.md` mtime is 2026-07-28, but the codebase has had
  3 SSE-loading-derivation fixes since (PRs #99, #100, #95), and
  `docs/architecture.md` (2026-07-29) hasn't been re-validated against
  the streaming-derived loading model. Worth a quick tech-lead sweep.

> [applied 2026-09-08: verified — false alarm. docs/streaming.md:250-254
> and docs/architecture.md:134 already document these fixes explicitly
> (with gh# references matching commit content). Also: the referenced
> PR numbers are slightly off in this report — 240399b is PR #99
> (correct), but 950a5b4 is PR #98 (not #100) and f72c4e2 is PR #105
> (not #95). No doc action needed; noting the citation drift for the
> audit trail only, original text unmodified above.]
- **No stale plans.** `.claude/plans/` is empty except `README.md` —
  PR #128's plan cleanup landed cleanly and stayed clean.

## 1. Context-essentials drift

- **Rule 1 (no `fetch` in components)**: ✅ no violations.
  `grep -rE "fetch\(" src/components/` returns 0 matches; all `fetch(`
  calls live in `src/api.js`. Confidence: **high**.
- **Rule 2 (`src/api.js` is the adapter boundary; `onEvent(type, event)`)**:
  ✅ no violations. Confidence: **high**. No SSE-handling code in
  components since the post-W32 consolidation.
- **Rule 3 (`App.jsx` owns all state)**: ✅ no violations.
  `git log` shows the `updateLast` / outer-spread-inner-mutate pattern
  was explicitly documented in PR #126 (`4e2863f docs(context): document
  App.jsx outer-spread/inner-mutate pattern`) — that documentation is
  still load-bearing, and no subsequent fix has contradicted it.
  Confidence: **high**.
- **Rule 4 (`react-markdown` is the only renderer)**: ✅ no violations.
  `dangerouslySetInnerHTML` not present anywhere in `src/`.
  Confidence: **high**.
- **Banned pattern: `--no-verify`**: ✅ zero matches in `git log
  --since="60 days ago" --grep="--no-verify"`. Confidence: **high**.
- **Banned pattern: `innerHTML` string matching for XSS safety**: ✅ no
  violations; only reference is a *comment* in
  `src/components/Markdown.test.jsx:14` explicitly explaining the
  prohibition (good — explicit anti-pattern documentation).
  Confidence: **high**.
- **Self-Learning Hard Rules (new in CLAUDE.md as of PR #146 / baf2074)**:
  ✅ no amendment/force-push commits observed since the rule was
  added2026-08-23. Confidence: **high** (only 7 days of post-rule
  data — recheck at W36).

> [applied 2026-09-08: rechecked in W36 §1 — still clean, same result]

## 2. Recurring `/fix-review` themes

- **None identified.** Of the 20 most recent merged PRs, every one
  touched either `src/` (rare) or pure `.claude/`/dependency surfaces.
  The `/fix-review` workflow shows consistent `0 findings · 0
  confirmed · 0 dismissed · 0 deferred` for all8 non-`src/` PRs
  (PRs #117, #128, #134, #140, #145, #146 + all dependabot bumps).
  The last substantive review round with full3-reviewer model fan-out
  was PR #127 (strategy picker) on 2026-08-06 — also clean. No
  recurring theme to promote.
- **Diff-shape gate observation**: REVIEWER_COUNT=1 routing for
  non-`src/`/`.claude/skills/`/`.claude/agents/` diffs is working as
  designed (PR #91, `922ce06 chore(dx): run one reviewer for non-src/
  diffs in fix-review`). The single-reviewer verdict has held up
  across 8 consecutive PRs. No false-negative pattern flagged.
  Confidence: **high** that this is genuine steady state, not a
  detection gap (the gate deliberately routes low-surface diffs to a
  cheap check, and arbiter scans independently).

## 3. Stale plans

- **No stale plans.** `.claude/plans/` contains only `README.md` — the
  directory was cleared by PR #128 on 2026-08-06 and no plan has been
  written since. Confidence: **high**.

## 4. Agent-memory health

- **`test-generator/MEMORY.md` + `vmm-rada-web-ui-quirks.md`**: only
  agent memory present.
  - mtime: 2026-07-27 (34 days old, threshold for "stale" is 60d —
 not yet stale).
  - 3 entries, all project-specific Vitest/RTL quirks. No
    contradictions with context-essentials. No duplicates across    other agents.
  - All3 entries are actively load-bearing:
    - "vi is global" → applies to every test file written by the
      agent.
    - "Final Answer" ambiguous match → applies to Stage3 tests.
    - "Stage components are plain prop-driven" → shapes the
      single-`render()` test pattern.
  - Suggest: no action. Recheck at W37 if no test work lands.

> [skipped 2026-09-08: explicit "no action" per report's own suggestion;
> re-confirmed in W36 §4 (41 days, still under 60-day threshold), still
> deferred to W37]
- **No memories** under `bug-fixer`, `code-simplifier`, `docs-maintainer`,
  `security-reviewer`, `tech-lead`, or `static-analysis`. With this
  project's mature workflow and `/fix-review` consistently returning
  zero findings on real PRs, there is little for them to memorize —
  this is the expected steady state, not a gap. Confidence: **medium**
  (technically possible an agent produced a finding on a `src/` PR
  that didn't get committed to memory; would need per-PR review).

## 5. Skill / agent inventory

- **11 skills** under `.claude/skills/` — all defined in `CLAUDE.md`,
  consistent with the global list. Cross-reference clean.
- **9 agents** under `.claude/agents/` — all defined in `CLAUDE.md`,
  consistent with the global list. No orphan agents introduced or
  dropped since last pass.
- **Overlap check**:
  - `code-simplifier` (behaviour-preserving JS refactor) vs.
 `static-analysis` (ESLint cosmetic) — different concerns (logic vs lint). No overlap.
  - `code-generator` (full issue lifecycle) vs. `bug-fixer` (one-bug
    fix) — explicit boundary in agent descriptions.
  - `security-reviewer` vs. `tech-lead` — security-reviewer is
    report-only, tech-lead has authority. Clean.
 - `docs-maintainer` is reportedly **broken** per project memory
    `project_docs_maintainer_agent_broken.md` (invalid tools
    frontmatter, Agent tool refuses to spawn). The skills and agents
    inventory is therefore effectively8-of-9 working agents. This is
    known and tracked — **not new in this pass**.
- **No skill obsolescence detected**: every skill under  `.claude/skills/` is referenced in `CLAUDE.md` workflow and has
  recent activity in the git log (last 90 days covers most).

## 6. Backend contract drift

- **`docs/streaming.md`** (mtime 2026-07-28) has not been updated since
  the front-end made three SSE-loading-derivation fixes:
  - PR #99 (`240399b fix(app): derive Stage 2/3 loading from prior
    stage completion`) — 2026-08-01ish
  - PR #100 (`950a5b4 fix(app): derive stage2Kind from persisted
    shape`) — same window
  - PR #95 (`f72c4e2 fix(app): restore pendingClarification on
    non-closed answer failures`) — same window
  These all describe derived state on the **client** side, so they
  don't change the wire contract; the backend doesn't need to know.
  But if `docs/streaming.md` describes the loading state model as
  backend-driven, the doc is now stale on the client interpretation.
  Confidence: **medium** — needs a content diff to confirm wording.
- **`docs/api-contract.md`** (mtime 2026-07-31) was last touched
  during the error-reference reconciliation (PR ~#115 era).
  No API-breaking changes from the frontend in this window. Safe.
- **`docs/architecture.md`** (mtime 2026-07-29) — same risk as
  streaming.md: the derived-state pattern changes the client state
  model but doesn't touch the wire. Doc may still be accurate; needs
  read to confirm. Confidence: **low** (no actual drift evidence).

> [applied 2026-09-08: read both docs directly — accurate, no drift.
> See TL;DR annotation above for full evidence.]
- **No new backend contract changes observed** in the front-end PRs
  examined (all diffs are within the adapter layer or derived state).

## 7. Patterns I noticed

- **Heavy dep-bump concentration**: of the 45 commits in the last
  90 days, ~22 (49%) are pure `build(deps-dev)` / `build(deps)`
  dependabot bumps. This is healthy (active security patching) but
  also means the project is heavily dependant on the global
  `dependabot-reviewer` agent working well — if it were broken or
  dropped, this stream would back up fast. Worth keeping a periodic
  health-check on `~/.claude/agents/dependabot-reviewer.md`.
  Confidence: **high**.
- **Single-reviewer routing is the dominant pattern now** — 8 of the
  last 8 non-`src/` PRs got `REVIEWER_COUNT=1`. The3-model
  fan-out is essentially dormant on housekeeping work. The
  `/fix-review` config (`.claude/skills/fix-review/`) gates this
  correctly via diff-shape, so this is by design — but it's a
  signal that the multi-model infrastructure is being exercised
  mainly as an arbiter signal, not a3-way vote. Not a problem
  (the `feedback-vote-count-not-verdict` rule from `common/memory/`
  makes the vote count a weak prior anyway), just an observation.
  Confidence: **medium**.
- **Backlog is invisible from this pass** — there are no open PRs or
  open issues in this scan window. The 2026-08-23 session mentioned
  "next steps: continue CI coverage integration for tests" as
  Check7 of `/housekeeping` with SKIP/PLANNED status. This has
  not progressed in the 7 days since. Confidence: **high**
  (consistently dormant since at least W30).
- **`/doubt-driven-development` workflow gate wired in PR #104
  (2026-07-ish)** — confirmed present in `CLAUDE.md` Workflow gates
  section. No usage evidence in PRs (it's an in-flight review  tool, not a PR-time check), so health is unverifiable from  dreaming alone.

## 8. What I couldn't tell (need manual review)

- **Content freshness of `docs/streaming.md` and
  `docs/architecture.md`** vs. the post-2026-07-28 derived-state
  changes. Mtime alone tells me nothing about the actual prose; a
  tech-lead review pass would catch any drift in10 minutes.
- **`docs-maintainer` broken-ness** — confirmed from existing memory
  but worth re-checking on a fresh session in case the tools
  frontmatter got fixed upstream. Low priority; the skill is
  effectively "do doc sync manually" right now.
- **`/apply-dreaming` adoption cadence** — only one apply per week
  (W30→W34 in the reports dir, plus W31 from PR #128), which is
  exactly what the README prescribes. Healthy.
- **CI coverage gap (Check 7 of `/housekeeping`)** — unresolved
  since at least W30 (~5 weeks). This is now genuinely a chronic
  item. Suggest raising it as a backlog issue or moving it from
  "PLANNED" to "OUT-OF-SCOPE" if it's been intentionally deferred.
- **Whether any non-`src/` PR since2026-08-23 actually had findings
  that the single-reviewer gate missed.** No false-negatives flagged,
  but the absence of findings on housekeeping PRs is partly
  tautological (these PRs rarely have findings to miss). Only
  substantive src-touching PRs in this window are the dreaming
  applies and one self-learn PR; all clean.
- **Whether `~/.claude/agents/dependabot-reviewer.md` is still
  healthy.** Not touched in this pass; assume healthy until
  contradicted.
