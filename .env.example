# Backend API URL. Defaults to http://localhost:8001 when not set.
# Set this when running the backend on a non-default host or port.
# Requires CORS_ORIGINS to be configured on the backend (llm-council-backend#18).
VITE_API_BASE=http://localhost:8001
