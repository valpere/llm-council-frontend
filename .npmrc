# eslint-plugin-react-hooks@7.x declares peerDep eslint ^9 only;
# no stable release supports eslint v10 yet. Use legacy-peer-deps
# until the plugin releases ESLint 10 compatibility.
legacy-peer-deps=true
